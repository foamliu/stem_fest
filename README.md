# 《如愿·看见》— stem_fest 制作仓库

**上汇实验学校 2026 年科技节 · 超写实真人质感短片**

> 本 README 是**项目总入口**：作品概览 / 目录 / 当前进度 / **生产参数与纪律** / 待办。
> 全项目**只有 4 份常读文档**：本文件 / `storyboard.md` / `ASSETS/README.md` / `ASSETS/STYLE/README.md`。
> 规则：**每个事实只写一处** —— 剧情与逐镜内容一律看 `storyboard.md`，素材台账看 `ASSETS/README.md`，
> 工具参数看 `mcp_server/`。本文件只留「怎么干活」。

---

## 1. 作品概览

| 项 | 值 |
|----|----|
| 片名 / 用途 | 《如愿·看见》· 上汇实验学校 2026 年科技节（公开展示，循环播放） |
| 规格 | **超写实真人质感短片（影视级真人风格）**，**126 个镜号**，片长约 **492 s（8 分 12 秒）**｜口径与逐幕明细见 `storyboard.md` §全片镜数核对 |
| 单镜时长 | 2–10 s，平均 **3.9 s**｜**一个镜号 = 一次 H3 生成 = 一个独立视频文件** |
| 主题 | 你们用命换来的今天，我们替你们看见了；科技要为人民服务，德才兼备，德是知道学一身本事为了谁 |
| 核心场景 | 上汇实验学校 八（1）班教室 |

**分镜脚本 = `storyboard.md`（唯一权威）**。含六幕逐镜表
（**镜号 / 时长 / 画面 / 运镜 / 景别 / 台词音效 / 参考图提示**）、时长定法、空间时间逻辑、黑屏字幕、音乐版权标注。

> 📖 **故事结构（六段 / 镜号 / 各幕时长）不在此复述** → `storyboard.md` §全片镜数核对。
> 本章所有数字口径与本文件 §3 的进度表，一律以那份表为准。

> **实际时长 = H3 量化后的播放时长**（帧数只能是 `17k+5`，见 §6.1 #13）：全片实际比剧本长约 **+7%**。
> 逐档换算表 / 定长规则见 `storyboard.md`「时长怎么定的」；**改台词后必跑** §9「自检命令」。

### 人物（7 位主角 + 4 位配角）

- **四小强**：刘思齐(01)、刘思成(02)、徐畅景(03)、张书扬(04)
- **穿越对象**：黄继光(05)、袁隆平(06)、钟南山(07)
- **`_extras/`**：小战士(08)、5 岁小女孩(09)、妈妈(10)、志愿军战士群演(11)

> 定妆照 / 造型基串 / seed / 出现镜号 → `ASSETS/README.md` §2；
> 逐镜台词与表情 → `storyboard.md`（不在此复制）。

---

## 2. 目录结构

```
stem_fest/
├── README.md                 # ★ 本文件：项目总入口
├── storyboard.md             # ★ 分镜脚本（126 镜，唯一权威）
├── ASSETS/                   # 输入素材（★ 图片不入库，目录结构入库）
│   ├── README.md             # ★ 素材规范 + 角色总表 + 场景/道具规格 + 看图与验收
│   ├── CHARACTERS/           # 角色定妆照（＋ _group/ 合影与单人图 ＋ _extras/ 配角）
│   ├── SCENES/               # 8 个场景 + _school_raw/（18 张实景原片池）
│   ├── PROPS/                # 12 个道具
│   └── STYLE/                # ★ 全片风格基准（风格后缀唯一出处）
├── OUTPUT/                   # ★ 产物不入库；**脚本(.py) / 文档(.md) 入库**
│   ├── _diag_act{0..5}_*.py  # 幕级批量生成（序幕一/二、第一~三幕、尾声）
│   ├── _run_all_acts.py      # ★ 六幕串行总编排（收尾补跑 _finalize_all.py）
│   ├── _make_group.py        # ★ 合影拼版入库（见 §4.2）
│   ├── _rebuild_4zn.py       # ★ 四小强+钟南山 专用拼版
│   ├── _diag_shot_duration…  # 时长复核 / 按角色统计 / 联系表 等常驻自检
│   └── group_ref/crops/      # 单人裁图（拼版原料，中文名；图片仍不入库）
├── workflows/                # 10 个 ComfyUI 工作流（API Format JSON；9 条已封装成工具，FireRed 留档）
└── mcp_server/               # MCP 工具服务器（2 个）
    ├── README.md             # ★ MCP 套件总览、注册配置、自检
    ├── comfyui_tools.md      # ★ 工具参数、约束、实测数据
    ├── comfyui_mcp_server.py # comfyui-drama：12 工具
    └── web_search_mcp_server.py
```

**命名规则（全 ASCII，小写 + 下划线）**

| 类型 | 规则 | 说明 |
|------|------|------|
| 角色 | `<slug>_hero_v01.png` | **每人物一张**，所有该角色镜头都引用它 |
| 场景 / 道具 | `<slug>_<view>_v<NN>.png` | view = `wide` / `medium` / `detail` / `hero` |

> ⚠️ 中文 / 空格文件名会被 ComfyUI 上传时改成随机 `mcp_ref_<hex>.png`，无法辨认。
> 图片文件被 `.gitignore` 忽略（`ASSETS/**/*.png|jpg|jpeg|webp`），`.md` 与 `.gitkeep` 入库
> ⇒ **"结构随仓库走、图片本地存"**；`OUTPUT/` 只入库**脚本（`*.py`）与文档（`*.md`）**，
> 渲染产物 / 日志 / 报告 txt / bak 仍由 `.gitignore` 挡在外面。

---

## 3. 当前进度（截至 2026-09-14）

### ✅ 已完成

| 模块 | 说明 |
|---|---|
| 分镜脚本 | `storyboard.md` **126 镜**定稿；时长按语速全片复核（**0 偏长 / 0 偏短**，37 镜人工锁定）；**运镜 + 景别 + 参考图提示三列已补全** |
| 角色定妆照 | **7/7** —— 袁隆平 v01(主)+v02(备)；黄继光 `hero_v02`(主，已去水印)+closeup |
| 配角定妆照 | `_extras/` 小战士去领章版、小女孩、妈妈、战士群演 |
| 学校实景原片 | `SCENES/_school_raw/` **18 张**，已分类 |
| 场景参考图 | **8/8**（06 战壕用**调色锁定** → `trench_wide_v02.png`） |
| 道具参考图 | **12/12**（seed 4001–4012） |
| **合影 / 多人参考图** | **26 组合 + 11 单人全部就位**（`_group/` **37 个文件**）★ 2026-09-14 改造：**程序拼版直接入库、不经图生图**（§4.2）；清掉 18 个废弃 `_labeled` |
| 钟南山定妆照 | ★ 2026-09-14 **换成写实照片版**（原插画风与四小强风格割裂）→ 6 张含他的参考图全部重拼（见 §8 P0 #3） |
| 音频素材 | 2 首主 BGM（现成音源）+ 5 条环境声/群杂（`ace_step_t2audio`），均体检通过；`（后期）` 标记全片铺满，`strip_late_audio()` 逐镜验证 |
| 生产脚本 | **6/6 幕全部就位**（见下表）；合影拼版脚本 2 个（`_make_group.py` / `_rebuild_4zn.py`） |
| **全片批量生成** | 🚀 **2026-09-14 07:23 启动**，`OUTPUT/_run_all_acts.py` **串行**跑 6 幕；跑完由 `_finalize_all.py` 自动补跑（见下）<br>状态：`OUTPUT/_run_all_state.txt` · 收尾：`OUTPUT/_finalize_state.txt` · 逐幕日志 `OUTPUT/_run_act<N>.log` |
| **画面污染整改** | ★ 2026-09-14 发现并修复 **14 处「元信息写法」**（镜 91 曾把舞台指示渲染成**画面字幕**）⇒ 见 §6.4；受影响的 12 镜已列入收尾重跑 |
| 钟南山水印 | ✅ **镜 79–105 已抽帧确认：未把「新华网」水印画进画面**（2026-09-14） |

### ⏳ 待办：**全片 126 镜视频生成**（2026-09-14 07:23 已启动，串行跑 6 幕）

> 🚀 由 `OUTPUT/_run_all_acts.py` 统一编排：**严格串行**（跑完一幕才启下一幕），
> 每幕前后核对队列清零（§6.1 #8 的纪律）。逐幕进度看 `OUTPUT/_run_all_state.txt`。

| 幕 | 镜号 | 镜数 | 脚本 | steps | 状态 |
|:--:|:--:|:--:|---|:--:|:--:|
| 序幕一 纸飞机 | 1–9 | 9 | `_diag_act0_plane.py` | 10（镜 4/5 建议 20） | 🚀 生成中 |
| 序幕二 启动 | 10–18 | 9 | `_diag_act2_startup.py` | 20 | 排队 |
| 第一幕 上甘岭 | 19–46 | 28 | `_diag_act1_trench.py` | 10（19/39/45 建议 20） | 排队 |
| 第二幕 禾下乘凉 | 47–74 | 28 | `_diag_act3_rice.py` | 10 | 排队 |
| 第三幕 餐车 | 75–105 | 31 | `_diag_act4_train.py` | 10（75/104 建议 20） | 排队 |
| 尾声 归来与揭晓 | 106–126 | 21 | `_diag_act5_finale.py` | 10（123-126 建议 4） | 排队 |

> ⚠️ **台词是否念对必须人耳验收**（或用 `qwen3_asr` 转写核对）。
> ⚠️ **史诗级耗时**：单条 3.4 分（4 步）～ 26 分（20 步），6 幕合计约 **13–16 小时**。
> ⚠️ **钟南山镜（79–105）首跑时留意「新华网」水印有无被画进画面**（§8 P0 #3）。

---

## 4. 工具链与「照抄即可」参数

### 4.1 MCP 工具

| 服务器 | 工具数 | 用途 |
|--------|:------:|------|
| `comfyui-drama` | 12 | 出图 / 出视频 / 配音 / 配乐 / 检测分割 / 上传参考图 / 取异步结果 |
| `web-search` | 6 | 网页搜索、新闻、图片搜索、抓正文、维基 |

> 📖 **逐工具参数 / 约束 / 实测数据 → `mcp_server/comfyui_tools.md`；注册与自检 → `mcp_server/README.md`。**
> 本文件只记**选型结论**（选哪条路线），不复制参数表。

| 选型结论 | 用什么 |
|---|---|
| ★★ **出视频（唯一入选路线）** | `video_minimax_h3_r2v`（≤2 张参考图：角色/合影 + 场景，角色锁定、只重绘一次） |
| 无角色动态镜（UI 动画 / 纯空镜） | `video_minimax_h3_t2v` |
| ★★ **图生图（唯一指定）** | `image_edit_longcat`：参考图 → 分镜图、换背景、老照片转换 |
| 场景 / 道具 / UI / 群像（**不保角色一致性**） | `z_image_turbo_t2i` |
| 环境声 / 群杂（L2 / L3，见 §4.4） | `ace_step_t2audio`（两首主 BGM 用现成音源，版权见 `storyboard.md`） |
| ⚠️ **当前流程不用** | `qwen3_tts`（台词写进 H3 prompt），仅备用 |
| ★ **查"画面里到底有没有某样东西"**（收尾抽查 / 逐镜验收） | `image_segmentation_sam3`：英文类别 → 框图 + 掩膜 + **每帧掩膜覆盖率**（`0` = 这帧没检出）；**可直接传 `.mp4`** 抽帧逐帧体检（用法见 `mcp_server/comfyui_tools.md` §6.6） |
| 上传 / 取异步结果 / 服务与队列自检 | `comfyui_upload_image` / `comfyui_get_result` / `comfyui_status` |

> 🔒 **铁律**：图生图只用 `image_edit_longcat`；**不**做"两版取更像"的 A/B 比较。
> ⛔ **但合影不在此列** —— 合影一律用**程序拼版**，见 §4.2。
> 排查第一步永远是 `comfyui_status` 确认 `reachable: true`。

### 4.2 H3 出视频与「参考图」照抄参数

| 参数 | **必须用** | 说明 |
|------|:---:|---|
| `steps` | **4（静态/单人/对话镜）/ 10（角色镜抽卡）/ 20（环境剧变镜）** | ⚠️ **不是一刀切**；MCP 签名默认值 `20` **不是配方**。<br>耗时（1056×608）：`4 步 ≈ 3.4 分` · `10 步 ≈ 9.5 分` · `20 步 ≈ 18–26 分`（近线性）<br>★ 镜 4「纸飞机掠过校园」实测 `4 步 ❌ → 10 步 ❌ → 20 步 ✅`（航拍跟拍、画面持续剧变） |
| `megapixels` | **0.6** | → 1056×608；0.92(720p) **必 OOM**。要 1080p 生成后超分 |
| `aspect_ratio` | `"16:9 (Widescreen)"` | |
| `duration` | 该镜时长（**照抄** `storyboard.md` 时长列） | `17k+5` 量化后实际播放多 0.04–0.58 s ⇒ **`6s` 实际比 `5s` 贵 1.41 s**，别随手写 6s。<br>换算表 / 定长规则 / 语速表 → `storyboard.md`「时长怎么定的」；公式见 §6.1 #13 |
| `ref_image_1` | 角色 / **对话双方合影** → `<Picture 1>` | 竖版头肩特写可直接用，**不必**裁 16:9 |
| `ref_image_2` | 该镜场景图 → `<Picture 2>` | 无角色镜走 `t2v` |
| `prompt` | `CUT 1:` + **景别**（照抄 `storyboard.md` 的景别列）+ 动作 + `Audio:` 台词原文 | 环境音不一定生成，见 §4.4；一镜一个主景别，定义见 storyboard「景别列读法」 |

**★ 参考图规则（2026-09-13 修订：按"在场人数"给图）** ——
**画面里应该出现几个人，参考图就必须含几张脸。** 只给说话人一张脸，其余人会消失、背景空荡。

| 画面里该有 | 参考图 |
|---|---|
| **4 人** | **四人合影**（`four_students`） |
| **2 人**（一对一对话） | **二人合影**（按组合拼版） |
| **1 人**（特写/反应镜）但场景里还该有别人 | **单人定妆照 + 四人合影**（双图位） |
| 确实只有 1 人、无背景人物 | 单人定妆照 + 场景参考 |
| 纯场景（无人） | 场景参考，走 T2V |

> ⚠️ **场景参考图不要用空教室**（模型会优先渲染空背景，人物像贴上去的）；面部特写镜例外，单人照即可。
> ⇒ 全片多人镜 **88/126**、去重 **28 种组合**（组合 ↔ 镜号台账见 §9）。
> 📖 该规则的完整版见 `storyboard.md`「参考图提示列读法」，不在本文件复述。

**★ 合影怎么做（2026-09-14 定案，用户实测结论）—— 只做「拼版」，不经过任何图生图**

> ⛔ **不要再用 `image_edit_longcat` 合并合影**：实测**有些转后身份失真**（部分人脸被改掉）。
> ✅ **做法**：定妆照裁人 → **程序并排拼版** → **拼版图本身就是成品**，直接入库 `ASSETS/CHARACTERS/_group/` 喂 R2V。

```powershell
# 单人裁图放 OUTPUT/group_ref/crops/<角色名>.png（中文名，脚本按名字取图）
py -3.10 OUTPUT/_make_group.py --list                      # 列组合与产物路径（不写文件）
py -3.10 OUTPUT/_make_group.py liu_sicheng_zhong_nanshan   # 只拼指定组合
py -3.10 OUTPUT/_make_group.py --all                       # 全部组合 + 全部单人图
py -3.10 OUTPUT/_make_group.py --force --all               # 覆盖已存在的产物
```

- 组合清单（**27 组**）与**从左到右站位**都写死在 `OUTPUT/_make_group.py` 的 `COMBOS` 里，**脚本是唯一出处**
- 产物名 `<slug>_hero_v01.png`，**无需 `_labeled` 版**（拼版不产生文字）
- **`KEEP_USER_SUPPLIED`（脚本内）保护 5 组用户提供的宽幅合影**不被拼版覆盖 ——
  尤其 `four_students` 保留 `four_students_hero_v02.png`（v01 混脸已报废）；要强制拼版得先手动删用户版
- ⚠️ **`four_students_zhong_nanshan` 走专用脚本 `_rebuild_4zn.py`**（2026-09-14）——
  拼的是「**写实四人合影 + 新写实钟南山**」，不是 crop 竖版单人图（比例对不上宽幅四人图）。
  旧用户版里是插画风带蓝底 + 姓名标签 + 豆包水印，已弃用
- ⚠️ **钟南山 `hero_v01` 带「新华网 WWW.NEWS.CN」水印**，会出现在每张含钟南山的参考图里；
  首跑钟南山镜时留意 H3 有无把该文字画进画面（§8 P0 #3）

### 4.3 ★ 看图与验收（三条路，按优先级）

1. **主模型直接读图**（最快）—— ⚠️ 额度 ≈ **7 张/会话**，超出后**静默丢弃**，且 **Agent 无法自救**
   （不能自己"新开会话"恢复）。
   ✅ **强项**：一图覆盖多镜 —— 联系表实测 **1 张图 ≈ 6 镜 × 4 帧**（镜号 / 景别 / 主体 / 人数 / 服装 / 道具 /
   构图 / 明显崩图 / 明显字幕水印 / **品牌露出**都能给）。
   ❌ **弱项**：低对比度小字、乱码、**逐字精确**（实测字幕条只能读大意且会错字）、以及**任何数值**
   （相似度 / 覆盖率 / 响度）。
   省额度：只读关键的、大图先缩到 ≤1024 px（`OUTPUT/_view.py`）、**多张拼成 montage 再读**
   —— **"大而少"优于"小而全"**：宽幅横条能读大意，12 行小裁剪堆叠就只能判"有没有"。
   📖 **读图边界与两条硬规则 → 本文件 §4.5 E**。
2. **本地 `qwen3.5:latest`**（可脚本化的批量核查）—— 用法与踩坑见 `ASSETS/README.md` §7。
   ⚠️ **跑批期间绝对不要调它**：常驻 5.5 GB 显存会把 H3 挤爆。
3. **数值法**（客观可复现，额度耗尽时首选）—— 用 PIL 现算：整图 RGB 均值 / 饱和度、
   局部四角取样（背景明暗冷暖）、土壁 G/R 比、天空 std、主体亮部占比。
   **这种一次性脚本量完即删**，别囤在 `OUTPUT/`（`OUTPUT/` 入库只收**常驻**脚本与文档）。

> 📖 三条路的能力边界与实测踩坑（额度、位置偏置、调色锁定的量化方法）→ `ASSETS/README.md` §7。

### 4.4 ★ 音频三层分工（2026-09-13 定案）

**H3 只生成"可辨认的台词"，一切氛围声都在后期铺。**

| 层 | 内容 | 谁来做 |
|:--:|---|---|
| **L1 台词** | 角色的**明确台词**（有原文、要对口型） | ✅ **H3**（只此一项） |
| **L2 环境声** | 风声 / 铁锹声 / 蝉鸣 / 稻浪 / 铁轨声 / 键盘声 / 炮响 / 纸张翻动 | ❌ 不用 H3 → **`ace_step_t2audio`** |
| **L3 群杂人声** | 学生交头接耳（"讨论声"、walla） | ❌ 不用 H3 → **`ace_step_t2audio`** |

> **为什么**（实测）：H3 把 `Audio:` 里的一切都当"有人要说话"处理，且**听不进去"多人说话"的语义**
> —— 只会产出一段单声道、无空间感、听不清内容的糊状人声（镜 9「讨论声渐起」已踩坑）；
> 环境声更直接：实测 H3 前后段 ≈ **−50 dB**（生不出来）。
> 📖 **L3 的实测指标与照抄配方 → `mcp_server/comfyui_tools.md`**（`ace_step_t2audio` 一节）。

**⚠️ 剧本文本纪律**：`音效：` 列里**禁写"讨论声 / 人声 / 说话声"**这类会被 H3 当台词的词
⇒ 改用显式标记 **`（后期）`** 后缀，生成脚本的 `strip_late_audio()` 会在拼 prompt 时
**自动剔除该整句**，只把 L1 台词交给 H3。三个生产脚本均已内置该函数并在 `run_shot()` 里调用。

**`NO_SPEECH` 文案**（无台词镜必须挂，否则 H3 会幻觉出人声 —— 实测镜 4 出"啊"、镜 5 出整句胡话）

> ⛔ **旧版（2026-09-13 的"否定式指令"）已作废 —— 它本身就是污染源**：
> ```
> 【本镜不要生成任何可辨认的语音或对白。只允许环境音与物件音；
>  若画面有人群，也只是听不清的杂音，不要生成任何词句。】
> ```
> 2026-09-15 视觉检查**实锤**：H3 把这段"对模型说的话"当台词，**渲染成了画面字幕**
> —— 镜 5 底部出现「本镜不要生成任何可□旁白」。**H3 分不清"指令"与"台词"**（同 §6.4）。
> ⚠️ 括号里的字**各次读法不一**（`_diag_act0_plane.py` 记「可语午白」、`_scan_hazard.py` 记「可口旁白」）
> —— 这正印证 §4.5 E：**模型画出来的就是乱码，读图判不准逐字**；判"多了字"可以，判"多了什么字"要靠 OCR / 逐帧对比。

✅ **新写法：正向音景描述** —— 只说**有什么声音**，绝不说"不要什么"；音景本身**不含可读词句**：

| 幕 / 场景 | 模式（各幕脚本按各自场景定制，照抄脚本为准） |
|---|---|
| 室内（序幕一·教室） | `Audio: 安静的室内环境底噪；轻微的衣料摩擦声与呼吸声；远处传来模糊的人群杂音，听不清任何词句。` |
| 野外（第一幕·战壕） | 野外开阔地环境底噪 ＋ 风吹土壁与沙袋的气流声 ＋ 远处零星人声杂音 |
| 田野（第二幕·稻田） | 田野开阔地底噪 ＋ 风吹稻叶沙沙声与虫鸣 ＋ 远处零星人声 |
| 车厢（第三幕·高铁） | 车厢行进低频嗡鸣与轻微晃动 ＋ 环境底噪 ＋ 远处模糊人声 |
| 夜晚教室（尾声） | 安静教室夜晚底噪 ＋ 笔记本风扇轻转 ＋ 衣料摩擦声与呼吸声 |

> 📌 **唯一出处 = `OUTPUT/_diag_act*.py` 的 `NO_SPEECH` 常量**（2026-09-15 六幕已全部改为正向音景）；
> 上表只是**模式说明**，不要从这里抄具体字句。
> 🔍 **改完必跑（两道）**：`_scan_hazard.py`（**粗筛**，列清单，有误报）→ `_verify_prompt_clean.py`
> （★ **闸门**：只校验**拼接后的真 prompt**，规则更严、**非 0 退出即禁止开跑** —— 以它为准）。
> 🗂️ **该重跑哪些镜**：`_plan_rerun.py` 按「视频 mtime < 修复时间 / 高危镜号 / 缺视频」判定 → `OUTPUT/_rerun_list.txt`。
> ⚠️ **它是粗筛，实测有噪声**（2026-09-15 复核 126 镜 = 97 命中，逐条核实后大部分是误报）：
> · **R6「制作备忘」94 镜几乎全是误报** —— `（后期）` 在 `strip_late_audio()` 阶段会被**整句剔除**，根本不进 H3 prompt；
> · **R3 会误伤词内"不可"** —— 如「嘴角几乎**不可**察地一挑」被当成否定祈使。
> ✅ **真阳性示例**：`GUARD_HUANG` 造型守卫串里的「**不可**改成其他军装、**不可**摘下军帽」——同一类问题，**待整改**。

### 4.5 ★ Agent 能力地图（**动手前先读**：能做什么 / 不能做什么 / 怎么取长补短）

> 由来：2026-09-15 做"视频感知 & 审查"能力评估时，Agent 一开始**误判了三处**（把"台词核对"当缺口、
> 把"音画同步"当成需要模型、把 ComfyUI 环境与 `py -3.10` 混为一谈）。此节把结论固化，
> **以后不要再重新推导一遍**（含"结论纠正"见 §4.5 G）。

**A. 已接线 · 现成能用**

| 能力 | 用什么 | 备注 |
|---|---|---|
| 出图 / 出视频 / 配音 / 配乐 | `comfyui-drama` **12 工具** | 参数/约束见 `mcp_server/comfyui_tools.md` |
| 联网查资料（含查工具、查文档） | `web-search` **6 工具** | 免密钥；只给 ~200 字摘要，长文配 `fetch_page` |
| 台词**文本**核对（念错/漏念） | `qwen3_asr` | **可直接喂 `.mp4`**（内部抽 16 kHz 音轨）→ 与 `storyboard.md` 逐字比对 |
| 画面"有没有某物 + 占多大" | `image_segmentation_sam3` | 图片**或视频抽帧**；给 `_bbox` / `_overlay` / `_mask` + 每帧 `mask_coverage`（0 = 没检出） |
| 读图（定性判读） | **基模自带视觉** | 额度 ≈7 张/会话；边界见 §4.5 E |
| **审查脚本**（**不是** MCP 工具，2026-09-15 起） | `OUTPUT/_subtitle_audit.py`（画面底部**字幕带**裁条拼图，一图塞 30+ 镜）、`_char_consistency.py`（镜头帧 **vs** 该镜定妆照**并排**，判"是不是同一个人"）、`_shot_refs.py`（镜号 → 参考图映射）、`_scan_hazard.py`（扫 prompt 里"对模型说的话"） | 这四个是"基模读图"的**放大器**：先把要判的东西做成**一张图 / 一份清单**，再交给读图或人工 |

**B. 节点/模型已经支持、但工具没接线**（⇒ 这是"缺接线"，不是"缺工具"）

| 项 | 断点（精确位置） | 接法（约 30 行） |
|---|---|---|
| ASR **词/字级时间戳** | ① `workflows/Qwen3-ASR 语音识别.json` 里 `Qwen3ASRLoader.forced_aligner` **写死 `"None"`**，且 MCP 工具未暴露<br>② `SaveText` 只接 `["2", 0]`（text）—— **时间戳在输出槽 2**，没接出来<br>③ `qwen3_asr` 结果整形只抓 `text`，返回里没有 `timestamps` | ① 选 `Qwen/Qwen3-ForcedAligner-0.6B`（`ComfyUI-Qwen3-ASR/nodes.py:116/159` 会自动下载）<br>② 加第二个 `SaveText ← ["2", 2]`，前缀 `{prefix}_ts`（避免与 text 同名）<br>③ 把时间轴提到返回顶层，并算派生指标：**首句起播秒 / 末句结束秒 vs 片长**（判"被片尾截断""前摇多长"） |
| ASR 模型档位 | `Qwen3ASRLoader.repo_id` 固定 `Qwen/Qwen3-ASR-0.6B`（**1.7B 也已在本地**，中文更好） | 工具加 `repo_id` 参数即可切 |
| SAM3 检测 | 已接线（2026-09-15） | 见 `comfyui_tools.md` §6.6 |

> ⚠️ 对齐器模型 `Qwen3-ForcedAligner-0.6B`（≈1.2 GB）**本机尚未下载**（HF 缓存里只有两个 ASR 模型）。
> 可提前下、**不占显存**：`hf download Qwen/Qwen3-ForcedAligner-0.6B --local-dir E:\code\ComfyUI\models\Qwen3-ASR\Qwen3-ForcedAligner-0.6B`
> （本机已确认有 `hf.exe` / `huggingface-cli.exe`；也可留给节点首启自动下载，或走 `source: ModelScope`）。

**C. 本机零安装就能做（不碰 GPU ⇒ 跑批期间也安全）**

ffmpeg **已实测**带这些滤镜：`blackdetect` / `freezedetect` / `silencedetect` / `ebur128` / `scdet` / `blend`；`ffprobe` 取规格。

```powershell
# ① 规格 + 音画时长差（同步性），② 黑帧，③ 静帧，④ 静音，⑤ 场景硬切，⑥ 响度
ffprobe -v error -show_entries stream=codec_type,codec_name,width,height,r_frame_rate,duration,nb_frames -of default=nw=1 <file>
cmd /c "ffmpeg -nostats -v info -i <file> -vf blackdetect=d=0.2:pix_th=0.10 -an -f null - 2>&1"
cmd /c "ffmpeg -nostats -v info -i <file> -vf freezedetect=n=-60dB:d=0.5    -an -f null - 2>&1"
cmd /c "ffmpeg -nostats -v info -i <file> -af silencedetect=n=-50dB:d=0.4   -vn -f null - 2>&1"
cmd /c "ffmpeg -nostats -v info -i <file> -vf scdet=threshold=10            -an -f null - 2>&1"
cmd /c "ffmpeg -nostats -v info -i <file> -filter_complex ebur128=peak=true      -f null - 2>&1"
```
> PS 里必须用 `cmd /c "… 2>&1"` 包一层（ffmpeg 日志走 stderr，否则抛 `NativeCommandError`）→ §6.5 #8。

**实测样例**（`OUTPUT/01_paper_plane/video/01_girl_mother_at_school_gate_00001_.mp4`，2026-09-15）：
1056×608@24fps / 73 帧；视频 3.0417 s、音频 3.0420 s（**音画差 0.3 ms**）；无黑帧 / 无静帧 / 无静音 / 无硬切；
**I = −10.3 LUFS、LRA = 20.0 LU、真峰 −2.1 dBFS**（响度偏高、动态过大 ⇒ 后期要压）。

**D. 没有的能力 → 获取方式**（2026-09-15 调研，全部开源离线方案；**装到 `py -3.10`**）

| 缺口 | 方案 | 获取 |
|---|---|---|
| 画面文字 **OCR**（字幕污染 / 水印，要求逐字可复现） | **RapidOCR 1.4.4**（Apache-2.0，纯 CPU onnxruntime） | `pip install rapidocr-onnxruntime` |
| **镜头硬切点**（秒） | **PySceneDetect** | `pip install scenedetect`（或 `scenedetect-headless`） |
| **人脸相似度**（跨镜角色漂移量化） | **InsightFace / ArcFace** | `pip install onnxruntime insightface` |
| **多帧视频理解**（时序 / 动作 / 运镜） | ① **ollama**：`ollama pull qwen3-vl:8b`（本机 0.34.0 ≥ 官方要求 0.12.7 ✅）<br>② **ComfyUI-QwenVL** v2.3.2（Qwen3-VL 2B~32B / Qwen2.5-VL / GGUF，**原生吃视频帧序列**，带 token 预算守护；附 `qwenvl_cli.py` 可命令行直调） | ② `git clone` 到 `ComfyUI/custom_nodes` + `pip install -r requirements.txt`（**跑批结束后再装**：抢显存） |
| 说话人分离（谁在说） | pyannote（需 HF token） | **低优先**：H3 单/双人镜用不到 |

**D-2. 已有但常被忽略的本地 VLM**：ollama `qwen3.5:latest`（9.7B Q4_K_M，`capabilities = vision / completion / tools / thinking`，ctx 262144）、`llava:latest`（7B vision）。
用途：**批量**帧描述（Agent 不在会话里也跑得动）；⚠️ **跑批期间禁用**（§6.1 #11，5.5 GB 常驻会挤爆 H3）。
（`gemma3:4b` / `gemma3:1b` 在 ollama 里只报 `completion`，别当视觉模型用。）

**E. 基模读图的边界（实测，2026-09-15；3 张真实审查材料）**

| ✅ 能读 | ❌ 读不准 / 读不到 |
|---|---|
| 联系表一图覆盖 **6 镜×4 帧**：镜号、景别、主体、人数、服装、道具、环境、明显崩图<br>★ **实锤**：2026-09-15 就是靠视觉检查发现**镜 5 / 镜 19 的底部字幕污染** | **低对比度小字 / 乱码 / 逐字精确**（实测字幕条 `_review_zoom/s91_bar.jpg` 只能读大意且有错字 ⇒ 判"**多了字**"可以，判"**多了什么字**"要靠 OCR / 逐帧对比） |
| 明显字幕、水印、**品牌露出**（实测发现校服上疑似 adidas logo） | **音频**（我听不到）、**视频时序**（我放不了视频）、**帧号 / 秒数** |
| 构图 / 景别 / 画面关系的**定性**判断 | **相似度、覆盖率、响度、切点秒数**等**任何数值** |

> ⇒ **两条硬规则**：① 验收标准**必须落在数值上**（同 §6.1 #6：不能只看 ComfyUI 报 `success`）；
> ② **读图只作线索、不作判据**，且**不可复现**（同一张图两次读可能不一致）。

**F. 两套环境，别搞混（★ 最容易出错的地方）**

| 用途 | 环境 | 说明 |
|---|---|---|
| MCP 服务器 / 一次性脚本 | **`py -3.10`** | 新依赖装这里。当前**没有** `cv2` / `onnxruntime` / `scenedetect` / `rapidocr` / `whisperx`（有 `numpy` / `PIL` / `huggingface_hub`） |
| ComfyUI 节点 | **ComfyUI 自带环境** | `pip install -r requirements.txt` 要装进 **ComfyUI 的环境**；模型放 `ComfyUI/models/...`（例：`models/Qwen3-ASR/`、`models/checkpoints/sam3.1_multiplex_fp16.safetensors`） |

⇒ 结论：**说"本机装没装某包"必须指明是哪套环境**（这次评估就在这儿栽过）。

**G. 本次评估纠正的三个结论（防止重复推导）**

| ❌ 错误结论 | ✅ 事实 |
|---|---|
| "台词核对是缺口" | **已有**：`qwen3_asr` 可直接喂 `.mp4` 做文本核对 |
| "台词时间轴要装 WhisperX 做强制对齐" | **不需要**：ComfyUI-Qwen3-ASR 原生支持 `Qwen3-ForcedAligner-0.6B` 出**词/字级时间戳**，只是工具没接线（§4.5 B） |
| "音画同步要用模型检测" | **不需要**：H3 是**音视频同一次生成**，天然同步；只需 `ffprobe` 比**音画时长差**（实测 0.3 ms） |


## 5. 内容层面的铁律（★ 违反必出废片）

> 📖 **逐条事实与出处（性别 / 年龄 / 口音 / 稻田不是麦田 / 徐畅景不戴眼镜 / 选图标准 …）
> 一律以 `storyboard.md` §角色性格与定位 · §空间与时间逻辑总览 及各幕逐镜表为准，不在此复述。**

| 铁律 | 违反后果 |
|---|---|
| ★★ **定妆照 = 外形的唯一权威** | prompt 写体形 / 脸型 / 胖瘦 / 年龄感 → 生成结果会**真的变脸变胖**（镜 15 踩过：写"圆脸、偏胖"，实为瓜子脸） |
| ★ **全片风格 = 超写实真人质感** | 出图 prompt **必须带「风格后缀」**（唯一出处 `ASSETS/STYLE/README.md`）；**不是**卡通 3D / 二次元 / CG |
| ★ **写 prompt 用"外观描述"代替年龄数字** | 写"83 岁"会画出苍老；写"精神矍铄、面容紧致、不显老"才对 |
| ★ **分镜里的性格/印象描述不可当外形依据** | "胖胖的""说话慢"是性格写法，外形只能看定妆照 |
| ★ **能裁就别修** | 画面内文字/水印落在空白区时**直接裁掉那条带**最可靠；任何"拟合填充"都留伪影。<br>裁前量"可裁边界"：**文字下界 < 裁切线 < 主体上界** |
| ★ **"自动检测画面内文字"会误伤人脸** | 通用"深色小连通域 + 聚类"会把**发丝边缘**当文字。<br>正解 ＝ **先区域约束定位** + **强校验「框外像素与源图逐像素完全相同」** |

> ⚠️ **视觉年龄 ≠ 实际年龄**：袁隆平 1961 年实际 31 岁（"21–25 岁"读数是符合设定的青年形象）；
> 钟南山 2020 年实际 83 岁但本人显年轻 → **不要画苍老**。
> 📖 **素材侧其余约束**（全 ASCII 命名 / 跨格式哈希不可比 / 相似度阈值 / 三视图风险 / 同名 `(1)(2)(3)` 必比对）
> → `ASSETS/README.md`。

---

## 6. 已知坑与硬约束（都是实测踩出来的，别重犯）

### 6.1 ComfyUI / 工具侧

| # | 约束 | 说明 |
|:-:|------|------|
| 1 | ★★ **定妆照 = 外形的唯一权威；prompt 禁止改动外形** | 造型基串只能写**客观可见的稳定特征**（性别 / 发型发色 / 服装 / 配饰）。**严禁**写体型、脸型、胖瘦、年龄感。<br>**反面案例**：镜 15 prompt 写了"圆脸、偏胖"（定妆照实为瓜子脸）→ 生成结果**胖一圈、脸变圆** |
| 2 | **H3 `megapixels` ≤ 0.6** | 16GB VRAM 下 0.92 必 OOM；要 1080p 生成后超分 |
| 3 | **H3 R2V 最多 2 张参考图** | ≥2 人镜 ⇒ 必须**一张合影**。<br>★ **合影 = 程序拼版，不经图生图**（2026-09-14 定案）：`image_edit_longcat` 转过的合影**有些身份失真** ⇒ 用 `OUTPUT/_make_group.py` 拼版直接入库。做法见 §4.2 |
| 4 | **角色镜必须引用定妆照** | 否则每张脸随机变化，定妆照白做 |
| 5 | **上传不覆盖同名文件** | 同名存为 `名字 (1).png`。**换图后先删 `E:\code\ComfyUI\input\名字.png` 再上传** |
| 6 | **输出规格以 `ffprobe` 实测为准** | 不要相信注释里的分辨率 |
| 7 | **长任务用 `wait=False`** | 先拿 `prompt_id`，再用 `comfyui_get_result` 取回 |
| 8 | ★ **启批处理前先看清任务数；跑完确认进程退出 + 队列清零** | 曾发生：脚本实参是 8 个镜头却被当成 2 个，且**进程后台存活**，LongCat 首帧任务与 H3 视频任务在队列里**交替执行**，每个还要重载 11.96 GB 模型 ⇒ 纯浪费。<br>**纪律**：① 启脚本前 `--dry` 看清任务数；② 跑完用 `Get-CimInstance Win32_Process` 核进程、查 `/queue` 与 `/history`；③ 不同 batch 输出前缀要可区分 |
| 9 | ★ **清队列姿势** | 删**排队中**：`POST /queue {"delete":[prompt_id]}`；<br>中断**正在执行**：先 `POST /interrupt`（空 body）再删队列 —— **只发 delete 删不掉 running 那个**；<br>清历史：`POST /history {"clear":true,"delete":[id,…]}` |
| 10 | ★★ **`steps` 按镜头类型选（4 / 10 / 20），不照抄签名默认值** | 见 §4.2。**耗时的真正决定变量是 steps × 帧数**，不是"模型比显存大" |
| 11 | ★ **跑批期间不要调 ollama 视觉模型** | `qwen3.5` 常驻 5.5 GB，会把显存挤到 76 MiB、功耗掉到 30 W。<br>用完必须 `Stop-Process -Id <llama-server PID> -Force`（`ollama stop` 会卡在 `Stopping...`） |
| 12 | ★ **H3 输出分辨率由 `ResolutionSelector` 反推** | `√(megapixels×1024²/(16×9))` 取 32 的整数倍：`0.6→1056×608`、`0.4→832×480`、`0.3→736×416`、`0.2→608×320` |
| 13 | ★ **帧数只能是 `17k+5`** | 公式 `max(5, round(时长×24)) + (5 - (max(5, round(时长×24)) % 17)) % 17`（`workflows/video_minimax_h3_r2v.json` 节点 131）<br>⚠️ 旧文写的「5s→136 帧 / 6s→136 / 7s→170」**是错的**，实测 `124 / 158 / 175` |
| 14 | **TTS `custom_speaker_name` 必须为空** | 仅在使用 `qwen3_tts` 时适用；该字段是音色 mixing 列表，填自定义名 → `ValueError` |
| 15 | **H3 不一定生成环境音** | 镜 15 实测：只念了台词，前后段 ≈ −50 dB ⇒ **环境音另做**（见 §4.4 三层分工） |
| 16 | ★ **"画面里有没有某物"别再靠肉眼猜** | 用 `image_segmentation_sam3`（SAM3 开放词汇检测）：图片**或视频抽帧** → 框图 + 掩膜 + 每帧 `mask_coverage`（**`0` = 没检出**）。调参口诀与验收口径 → `mcp_server/comfyui_tools.md` §6.6<br>⚠️ 每次提交都会换模型（1.6 GB），**跑批期间只用单图**，别拿它扫长视频 |
| 17 | ★ **基模读图 ≠ 验收依据** | 额度 ≈ **7 张/会话**（超出静默丢弃，Agent 无法自救）、**不可复现**、读不准低对比度小字与任何数值 ⇒ 读图只作**线索**，判据一律用**数值**（见 §4.5 E） |
| 18 | ★ **两套 Python 环境别搞混** | MCP 服务器/脚本 = **`py -3.10`**；ComfyUI 节点 = **ComfyUI 自己的环境**。查"装没装某包"必须说明在哪套里（见 §4.5 F） |

### 6.2 ★ **合成合影 / 多人参考图**（唯一新增的系统性坑）

⛔ **不要用任何图生图把多张脸合成一张合影** —— `image_edit_longcat` 实测**有些转后身份失真**。
✅ **正解**：程序并排拼版，拼版图本身就是成品。做法与台账见 **§4.2 / §9**。

> 📖 **素材侧的其余约束**（全 ASCII 命名 / 跨格式哈希不可比 / 归一化相似度阈值 / 三视图拼版风险 /
> 同名 `(1)(2)(3)` 入库前必比对）→ `ASSETS/README.md`（命名硬规则 · 版本控制 · 待补素材）。

### 6.4 ★★ **prompt 禁止写「元信息」**（写进画面描述会被渲染成字幕）

**事故（2026-09-14 镜 91）**：prompt 写了舞台指示
> 「眼神很深、表情看不出情绪，**像在消化一件很难立刻接受的事**」

成片画面底部**真的出现了这行中文字幕**。原因：模型无法表演「像在消化…」这种
**内心状态 / 镜头用意**，它能做的只有"把这句话显示出来"。

**事故二（2026-09-15 镜 5 / 镜 19）—— 比镜 91 更隐蔽**：连**否定式指令**也会被渲染。
- 镜 5 底部字幕「本镜不要生成任何可□旁白」← 来自 `NO_SPEECH` 常量（当时是「本镜不要生成任何…」）
  <br>⚠️ `□` 处**逐字读不准**（两个脚本分别记成「可语午白」「可口旁白」）⇒ 印证 §4.5 E：判"多了字"可以，判"多了什么字"要靠 OCR
- 镜 19 底部字幕「全镜高适」← 疑似同类幻觉（同样存在逐字不确定性）

⇒ 根因统一为：**prompt 里出现"对模型说的话"**（本镜 / 不要生成 / 只允许 / 若画面…），
H3 分不清"指令"与"台词"，一律当词句输出。
📌 **对策**：`NO_SPEECH` 已改成正向音景（见 §4.4）；全片 prompt 用脚本扫一遍（见下）。

| 写法 | 判定 |
|---|:--:|
| ✅ 摄影机拍得到的：动作、表情、景别、光线、环境、人数、衣着 | 安全 |
| ❌ **比喻式内心状态**：像在…／仿佛…／像是…／显得… | **会被画成字幕** |
| ❌ **镜头用意**：暗示…／表达…／说明…／观众要读到…／这是情绪高点 | **会被画成字幕** |
| ❌ **镜头编号指代**：本镜／该镜／这一镜 —— ⚠️ **`NO_SPEECH` 里的「本镜不要生成…」也曾被豁免，2026-09-15 实锤它照样被渲染成字幕 ⇒ 豁免取消，改正向音景（§4.4）** | **会被画成字幕** |

**修法**：改写成可见的物理表现。
```
✗ 像在消化一件很难立刻接受的事
✓ 面无表情地沉默了几秒，喉结动了一下
```

**自查命令**（改完任何 prompt 后跑）：
```powershell
py -3.10 OUTPUT/_scan_hazard.py          # ① 粗筛：6 条规则扫"对模型说的话"（清单落 OUTPUT/_prompt_hazard.txt）
                                         #    ⚠️ 有噪声：R6 的「（后期）」会被 strip_late_audio() 剔除（误报）；
                                         #      R3 误伤词内"不可"（如「几乎不可察」）⇒ 命中需逐条人工核实
py -3.10 OUTPUT/_verify_prompt_clean.py  # ② ★ **闸门**：只校验**拼接后的真 prompt**（含 + CONST 展开），
                                         #    规则更严、非 0 退出即禁止开跑 —— 以这个为准
py -3.10 OUTPUT/_scan_prompt_meta.py     # 历史脚本：只扫"元信息"写法，应为 0 处
```

> 其他已验证的画面污染源：参考图里的**文字**（如人名标签、水印）也可能被画进画面。
> 本片钟南山定妆照带「新华网 WWW.NEWS.CN」水印 ⇒ **镜 79–105 已验：未泄漏**（2026-09-14 抽帧确认）。

### 6.5 系统 / 脚本环境侧

| # | 坑 | 解法 |
|:-:|----|------|
| 1 | 单条命令 **30 秒超时** | 长任务用 `Start-Process` 后台跑 + 轮询状态文件 |
| 2 | PS 5.1 读**无 BOM 的 UTF-8 `.ps1`** 按 GBK 解析 → 中文致语法错误 | 脚本改纯 ASCII，或用 Python |
| 3 | `Invoke-RestMethod` 发中文变 `???????` | body 用 `[Text.Encoding]::UTF8.GetBytes()`，或直接用 Python |
| 4 | `>` 重定向在 PS 5.1 写 **UTF-16** | 用 Python 写 UTF-8，或 `Out-File -Encoding UTF8` |
| 5 | **robocopy 会预分配目标文件** | 中断后是"长度正常但内容为零"的假文件；**重拷前先删目标同名文件** |
| 6 | `qwen3.5` 不关 thinking 会返回**空字符串** | 请求体加 `"think": False` |
| 7 | 控制台中文乱码 ≠ 文件损坏 | PS 5.1 用 ANSI 读 UTF-8 的显示问题；用 `read_files` 或 Python 复核 |
| 8 | `ffmpeg` 把日志写 **stderr**，PS 里 `2>&1` 会抛 `NativeCommandError`（命令"失败"但其实跑完了） | 用 `cmd /c "ffmpeg … 2>&1"` 包一层（照抄见 §4.5 C），或先 `$ErrorActionPreference='SilentlyContinue'` |

> 📖 **ollama 侧的两条**（`ollama pull` 进度条报错 / 桌面版模型路径不读环境变量）→ `ASSETS/README.md` §7。

---

## 7. 推荐生产流程（逐镜）

```
① 查 storyboard.md 该镜「参考图提示」列 → 确定要什么素材
② 出分镜图：有角色 → image_edit_longcat；无角色 → z_image_turbo_t2i
   · image = 定妆照 / 合影（≥2 人一律用拼版合影，见 §4.2）
   · prompt = 景别（必须写死）+ 场景 + 表情动作 + 风格后缀（勿用文字描述长相）
   · seed = ASSETS/README.md §2「固定 seed」｜ megapixels = 1.5（→1680×944）
   · ★ 验收：输出画面脸高 ≥ 250 px
③ 出视频（音频这步一并生成，**不要另做配音**）
   · 有角色 → video_minimax_h3_r2v；无角色 → t2v
   · ref_image_1 = 角色/合影 → <Picture 1>｜ref_image_2 = 场景图 → <Picture 2>
   · prompt = `CUT 1:` + 景别 + 动作 + `Audio:`（L1 台词原文；环境声另铺）
   · 参数照抄 §4.2（尤其 steps）
④ 音频：片段内 L1 台词 → H3 prompt；L2 环境声 / L3 群杂 → ace_step_t2audio（见 §4.4）
        跨镜 BGM → 现成音源后期铺整段（版权标注见 storyboard.md）
⑤ 剪辑：按镜号拼接（相邻用尾帧/首帧衔接）→ 铺 BGM → 音量平衡 → 循环播放
```

> ⚠️ **改任何台词后必须重跑** `python OUTPUT/_diag_shot_duration_audit.py` —— 时长是按字数算出来的。

### 7.1 分批跑幕：照抄即可的命令

每幕一个独立脚本（**互不引用**，避免 NameError），产物落在对应 `OUTPUT/<scene>/video/`，
报告落在 `OUTPUT/_act<N>_<scene>_report.txt`。幕 ↔ 镜号 ↔ 脚本 ↔ 目录的对照表见 **§3**。

```powershell
# ① 永远先 --dry：核对镜数 / 时长 / 参考图是否都在（§6.1 #8 的纪律）
py -3.10 OUTPUT/_diag_act1_trench.py --dry

# ② 跑全幕（后台，单条 3–26 分钟，整幕数小时）—— 必须后台，否则 30s 命令超时
Start-Process py -ArgumentList '-3.10','OUTPUT/_diag_act1_trench.py','--steps=10' `
  -RedirectStandardOutput OUTPUT/_act1_run.log -NoNewWindow

# ③ 抽查单镜（先用短镜验证管线，再放全量）
py -3.10 OUTPUT/_diag_act1_trench.py --steps=4 35

# ④ 跑完核对：进程已退出 + 队列清零（§6.1 #8）
Get-CimInstance Win32_Process -Filter "Name='py.exe'" | Select-Object ProcessId,CommandLine
py -3.10 -c "import json,urllib.request;q=json.load(urllib.request.urlopen('http://127.0.0.1:8188/queue'));print(len(q['queue_running']),len(q['queue_pending']))"
```

> ★ **第一幕（19-46）要点**：唯一场景图是 `trench_wide_v02.png`（**调色锁定版**，勿换回 v01）；
> 无台词镜（19、23、46）已挂 `NO_SPEECH`；**镜 19 / 39 / 45 属「环境剧变镜」**，建议单独补跑 `--steps=20`。
> 其余各幕要点写在各自脚本的文件头 docstring 里 —— **改脚本前先读它**。

**★ 全片一键串行跑（2026-09-14 起推荐）**

```powershell
# ① 先校验 6 幕镜数（合计应为 126）+ prompt 元信息自查（应为 0 处）
py -3.10 OUTPUT/_run_all_acts.py --dry
py -3.10 OUTPUT/_scan_prompt_meta.py

# ② 串行生成 6 幕
Start-Process py -ArgumentList '-3.10','OUTPUT/_run_all_acts.py' `
  -RedirectStandardOutput OUTPUT/_run_all_console.log -RedirectStandardError OUTPUT/_run_all_err.log -NoNewWindow

# ③ 收尾（自动等 ② 退出，再补跑「prompt 修复镜 + 提步数镜 + 降步数镜」）
Start-Process py -ArgumentList '-3.10','OUTPUT/_finalize_all.py' `
  -RedirectStandardOutput OUTPUT/_finalize_console.log -RedirectStandardError OUTPUT/_finalize_err.log -NoNewWindow

# 进度
Get-Content OUTPUT/_run_all_state.txt -Encoding UTF8 -Tail 20
Get-Content OUTPUT/_finalize_state.txt -Encoding UTF8 -Tail 20
```

**收尾补跑的三类镜**（`_finalize_all.py` 内写死）：

| 类 | 镜 | 说明 |
|---|---|---|
| A 提步数（环境剧变） | 4,5 / 18 / 19,39,45 / 74 / 75,104 | 10 或 4 步 → **20 步** |
| B 重跑（prompt 修复） | 6 / 10,16 / 23,33,38,43 / 52 / 91,94,96 | 改用「无元信息」的新 prompt |
| C 降步数（省机时） | 123,124,125,126 | 纯字幕/静帧/定格 → **4 步** |

---

## 8. 待办清单

### P0 — 阻塞项

| # | 事项 | 结论 / 备注 |
|:-:|------|------|
| 1 | ~~合影参考图（20 张）~~ | ✅ **2026-09-14 完成**。`_group/` 现 **37 个文件**（26 组合 + 11 单人图）。规则改为**程序拼版直接入库、不经图生图**（`OUTPUT/_make_group.py`），做法见 §4.2 |
| 2 | ~~补 `_extras/` 配角定妆照~~ | ✅ 2026-09-13 完成 |
| 3 | ~~钟南山定妆照是插画风~~ | ✅ **2026-09-14 已解决** —— 换成**写实照片版**，与四小强风格统一。<br>**受影响参考图全部重拼**：4 张二人合影（`liu_siqi_` / `liu_sicheng_` / `xu_changjing_` / `zhang_shuyang_zhong_nanshan`）、`solo_zhong_nanshan`、以及 **`four_students_zhong_nanshan`**（旧用户版里是插画风带蓝底 + 姓名标签 + 豆包水印，已用 `_rebuild_4zn.py` 改为「写实四人合影 + 写实钟南山」拼版，覆盖镜 81/82/84/89/91/92/94/105）。<br>⚠️ **遗留**：钟南山新图右下角带「新华网 WWW.NEWS.CN」水印 ⇒ 首跑钟南山镜（79–105）时留意 H3 **有没有把水印文字画进画面**；有泄漏再处理 |
| 4 | ★ **全片 126 镜视频生成** | 🚀 **2026-09-14 07:23 已启动**（`OUTPUT/_run_all_acts.py` 串行跑 6 幕，约 13–16 小时）。进度见 §3 / `OUTPUT/_run_all_state.txt`。<br>⚠️ 钟南山镜（79–105）首跑时留意「新华网」水印有无被画进画面 |

### P1 — 重要但不阻塞

| # | 事项 | 备注 |
|:-:|------|------|
| 5 | 补 `ASSETS/配乐提示词规格.md` | `ace_step_t2audio` 的文档引用了它，文件不存在；**L3 配方已另存于 `mcp_server/comfyui_tools.md` §6.5**，本文件可降级 |
| 6 | 确认黄继光另一张图归属 | `照片处理与AI 3D漫剧定妆照制作.jpeg`（哈希 `241428C3`）未入库 |
| 7 | 决定袁隆平 v01/v02 是否只留一张 | v01 中山装三视图 = 主参考；v02 备选 |

### P2 — 收尾 / 卫生

| # | 事项 | 备注 |
|:-:|------|------|
| 8 | 固化 `tools/*` | 现有 `OUTPUT/*.py` 都是「诊断脚本」，**用完即弃**；但 **`_make_group.py` / `_rebuild_4zn.py` / `_diag_shot_duration_audit.py` / `_diag_cast_per_char.py` 已是长期资产**，值得提到 `tools/` |
| 9 | 清理 `%TEMP%\vwpkmrz1` | 2.5 GB VS 安装器缓存，可安全删除（C 盘只剩 2 GB） |
| 10 | 决定是否删 `llava:latest` | 4.7 GB，已证实不可用 |

---

## 9. 合影组合 ↔ 镜号台账（R2V 硬前提；**唯一权威**）

> 生成方式：`OUTPUT/_make_group.py` 按 `COMBOS` 拼版 → `_group/<slug>_hero_v01.png`。
> 逐镜清单：`python OUTPUT/_diag_cast_per_char.py` → `OUTPUT/_cast_per_char.txt`。

| 角色组合（slug） | 镜号 | 镜数 |
|---|---|:--:|
| 四小强 `four_students` | 5,6,8,9,10,11,12,17,37,41,48,49,51,76,77,83,106,107,108,110,111,112,113,114,115,116,117,118,120,121,126 | 31 |
| 四小强 + 黄继光 `_huang_jiguang` | 24,27,29,33,38,40,43,45,46,109 | 10 |
| 四小强 + 袁隆平 `_yuan_longping` | 47,54,55,56,58,60,64,65,67,74 | 10 |
| 四小强 + 钟南山 `_zhong_nanshan` | 81,82,84,89,91,92,94,105 | 8 |
| 张书扬 + 钟南山 | 86,87,95,96,97,98,99 | 7 |
| 刘思齐 + 钟南山 | 85,93,100,101,102,103 | 6 |
| 张书扬 + 徐畅景 | 13,14,21,22,31 | 5 |
| 小战士 + 战士群演 + 黄继光 | 23,36,39 | 3 |
| 妈妈 + 小女孩 `girl_mother` | 1,2,3 | 3 |
| 张书扬 + 袁隆平 | 57,70,71 | 3 |
| 刘思齐 + 袁隆平 | 59,66,72 | 3 |
| 刘思成 + 袁隆平 | 61,62,63 | 3 |
| 刘思成 + 小战士 + 战士群演 | 34,35 | 2 |
| 刘思成 + 张书扬 | 16,119 | 2 |
| 刘思成 + 刘思齐 | 20,78 | 2 |
| 张书扬 + 黄继光 | 25,30 | 2 |
| 刘思成 + 黄继光 | 26,28 | 2 |
| 徐畅景 + 袁隆平 | 68,69 | 2 |
| 刘思成 + 钟南山 | 79,90 | 2 |
| 四小强 + 战士群演 | 19 | 1 |
| 刘思齐 + 张书扬 + 徐畅景 | 7 | 1 |
| 徐畅景 + 战士群演 + 黄继光 | 32 | 1 |
| 刘思齐 + 张书扬 | 15 | 1 |
| 战士群演 + 黄继光 | 42 | 1 |
| 刘思齐 + 黄继光 | 44 | 1 |
| 徐畅景 + 钟南山 | 88 | 1 |

> slug 一律 `<角色名_下划线相连>`（四小强开头用 `four_students`、配角用 `mother`/`girl`/`soldiers`/`young_soldier`），
> **完整对照表见 `_make_group.py` 的 `COMBOS`**。
> **不做这 2 组**：①「刘思成、刘思齐、钟南山」—— 镜 80 台词点名造成的假阳性，用 `liu_sicheng_liu_siqi`；
> ②「袁隆平、钟南山、黄继光」—— 镜 125 是三帧静帧，用三张单人图拼帧。

---

## 10. 环境信息

| 项 | 值 |
|----|-----|
| 项目根 | `E:\code\stem_fest`（MCP 相对路径都按**项目根**解析，与 cwd 无关） |
| ComfyUI | `0.33.0` @ `http://127.0.0.1:8188`｜`COMFYUI_ROOT = E:\code\ComfyUI`（⚠️ 出图前先启动，启动后约 1 分钟才连得上） |
| GPU | RTX 4090 Laptop **16 GB**（决定 `megapixels ≤ 0.6`） |
| ollama | `0.34.0` @ `http://127.0.0.1:11434`；模型库 `E:\ollama\models` |
| Python（**两套，别混**） | MCP 服务器 / 一次性脚本 = **`py -3.10`**（**没有** `cv2` / `onnxruntime` / `scenedetect` / `rapidocr` / `whisperx`；有 `numpy` / `PIL` / `huggingface_hub`）<br>ComfyUI 节点 = **ComfyUI 自带环境**（`pip install -r requirements.txt` 装那边；模型放 `ComfyUI/models/`） |
| **本机零安装能力** | `ffmpeg`（**已带** `blackdetect` / `freezedetect` / `silencedetect` / `ebur128` / `scdet`）、`ffprobe`、`hf.exe` / `huggingface-cli.exe`（可预下模型，**不占显存**） |
| **本地视觉模型** | ✅ ollama `qwen3.5:latest`（9.7B Q4_K_M，`vision`/`tools`/`thinking`，ctx 262144）、`llava:latest`（7B vision）｜⛔ `gemma3:*` 在 ollama 里只报 `completion`<br>⚠️ **跑批期间禁用**（§6.1 #11） |
| **已就位的权重** | `models/checkpoints/sam3.1_multiplex_fp16.safetensors`（1.6 GB，SAM3 检测）、`models/Qwen3-ASR/{0.6B,1.7B}`（ASR）<br>❌ **缺** `Qwen3-ForcedAligner-0.6B`（≈1.2 GB，拿到词级时间戳要用 → §4.5 B） |
| 磁盘 | C ⚠️ 2 GB / D 70.8 GB / E 47.2 GB |

**自检命令**

```powershell
python mcp_server/selftest.py              # ComfyUI 套件（离线）
python mcp_server/web_search_selftest.py   # 搜索套件（离线）
python OUTPUT/_diag_shot_duration_audit.py # ★ 分镜时长复核（改台词后必跑）
python OUTPUT/_scan_hazard.py              # ★ H3 prompt 字幕污染高危扫描（改 prompt 后必跑）→ §6.4
python OUTPUT/_subtitle_audit.py           # ★ 画面底部字幕带抽检拼图（交给读图/人工复核）
python OUTPUT/_diag_cast_per_char.py       # ★ 按角色统计 + 合影组合去重
python OUTPUT/_make_group.py --list        # 合影拼版清单（不写文件）
```

---

## 11. 文档索引

| 我想…… | 看这个 |
|--------|--------|
| 了解剧情 / 镜头 / 台词 / 运镜 / 景别 / 参考图 | `storyboard.md`（**唯一权威**） |
| 查某角色的定妆照、造型基串、seed、出现镜号；素材命名；怎么"看图"验收 | `ASSETS/README.md` |
| 查场景 / 道具图规格与生成参数 | `ASSETS/README.md` §7 |
| 画风 / 色调 / 风格后缀要对齐 | `ASSETS/STYLE/README.md`（**唯一出处**） |
| 查 MCP 工具怎么注册 / 怎么用 | `mcp_server/README.md` |
| 查工具参数、约束、实测数据、音频配方 | `mcp_server/comfyui_tools.md` |
| **判断"这件事 Agent/工具能不能做、该用哪个、缺什么、装在哪套环境"** | 本文件 **§4.5 Agent 能力地图** |
| **合影怎么做 / 组合 ↔ 镜号台账** | 本文件 **§4.2** 与 **§9** |
| **各幕批量生成脚本怎么用** | 本文件 **§3 / §7.1**，每个脚本的文件头 docstring |
