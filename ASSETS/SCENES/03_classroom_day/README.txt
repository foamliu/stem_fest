教室场景图（全片**唯一**的「傍晚 6 点」教室素材）

★ 选图结论（2026-09-21 补记）
────────────────────────────────────────────────
v01~v05 五张都是**同一间教室的不同视角**（差异在机位 / 景别，不在光线与陈设，
全套都是「傍晚 6 点、天还亮、暖金光 + 右窗日落」），**定版 = `classroom_day_wide_v01.png`（第一张）**
（用户指令：「如果非要选一张，那就第一张」）。

定版依据 = **机位**（因为光色一致，只能按「本幕需要什么」选）：

| 文件 | 机位 / 构图 | 用途 |
|------|------------|------|
| ★ `v01` | 正对讲台的**标准全景**：后墙板报 + 左窗 + 右窗日落，三面信息都在 | **定版**。镜 10-18「全班在全息设备前启动」的多人调度最合 |
| `v02` | 略偏右的中景，桌面更近 | 备用 / 局部镜 |
| `v03` | 偏左的侧视全景 | 备用 / 局部镜 |
| `v04` | **从走廊隔窗拍的外视角**（画面被窗框切分） | 专供「隔窗看教室」那类镜 |
| `v05` | 偏右的近景，桌椅占满画面 | 备用 / 局部镜 |

⚠️ 脚本里**写死了 v01**（见 `OUTPUT/_diag_act0_plane.py` 的 `SCENE_CLASS`
与 `OUTPUT/_diag_act2_startup.py` 的 `SCENE`）—— **改素材必须同时改那两行**，
否则等于没换素材。改完跑 `py -3.10 OUTPUT/_check_scene_refs.py` 自查。

★ 2026-09-21 合并说明
────────────────────────────────────────────────
本目录**同时承担原 `04_classroom_dusk` 的职责**（镜 10-18）。
原目录已物理删除，理由（用户原话）：
> 「故事的开始就是在现实世界的下午 6 点，本来就是 dusk，没必要分三个场景，过于的冗余」

合并后全片教室只剩**两种光**：
  · `03_classroom_day` = 傍晚 6 点、天还亮（暖金）→ 镜 5-9、**10-18**
  · `05_classroom_night` = 夜（深蓝 + 橙红、屏幕光打脸）→ 镜 106-126

收益：杜绝「同一间教室用两张不同场景图」带来的**跨幕光线一致性风险**
（序幕二镜 10-18 与尾声镜 106-126 是同一间教室、现实只隔一小会儿）。

镜 10-18 的历史产物已迁到 `OUTPUT/03_classroom_day/`（迁移脚本 `OUTPUT/_migrate_04_to_03.py`）。
