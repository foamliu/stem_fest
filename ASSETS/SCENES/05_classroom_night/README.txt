夜晚教室场景图（尾声 镜 106-126）

★ 选图结论（2026-09-21 补记）
────────────────────────────────────────────────
v01~v04 四张都是**同一间夜晚教室的不同视角**（夜色 + 空桌椅 + 后墙板报 +
暖橙壁灯的基调一致），**定版 = `classroom_night_wide_v01.png`（第一张）**
（用户指令：「如果非要选一张，那就第一张」）。

定版依据「本幕需要什么」——镜 106-126 是**尾声**，关键信息有两条：
① 孩子们围着**发光笔记本**（屏幕冷光打脸）② 窗外已暗、室内只余暖橙余光。

| 文件 | 机位 / 构图 | 用途 |
|------|------------|------|
| ★ `v01` | 正面偏左全景，**画面左前方就是那台亮屏笔记本** | **定版**。四张里唯一把「冷光屏幕」放在主体位置的机位，正对尾声「熬夜攻关」的戏 |
| `v02` | 更规整的正面全景（无人 + 无屏幕） | 纯空镜 |
| `v03` | 偏右近景 | 备用 / 局部镜 |
| `v04` | 隔门框的外视角（画面被门框切分） | 专供「隔门看教室」那类镜 |

⚠️ 脚本里**写死了 v01**（见 `OUTPUT/_diag_act5_finale.py` 的 `SCENE`）
—— **改素材必须同时改那一行**，否则等于没换素材。
改完跑 `py -3.10 OUTPUT/_check_scene_refs.py` 自查。
