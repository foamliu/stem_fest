# -*- coding: utf-8 -*-
"""⚠️ 已弃用（2026-09-20）—— 保留仅作**负面记录**。

**问题**：`_check_video_regions.py` 的输出被重定向到文件时，中文变成
`目录`→`鐩綍` / 更糟的叠加乱码，导致结果文件没法读。

**为什么「迭代反向转码」这条路走不通**（本文件起初就是这么写的，实测失败）：
  · 设想：把字节按 `GBK 解码 → UTF-8 编码` 反复迭代，直到恢复成「目录」。
  · 实测：**超线性膨胀** —— 2 个来回后文本就涨到 12 KB，6 个来回超时，
    30 个来回直接 `MemoryError`（每层汉字数近似翻倍）。
  · 且单步结果不稳定：同样输入，首字有时解得「目」、有时解得「嚙」⇒ 判据不可靠。

**正解（请照做）**：**别让它产生乱码** ——
  ```
  py -3.10 OUTPUT/_check_video_regions.py --shots=32,36,42,44 > OUTPUT/_chk.txt
  ```
  输出**不进控制台**（不经过 PowerShell 的 ANSI 解码管道），
  文件里就是正常的 UTF-8，直接读即可。
  本次实测：同样的命令，之前用 `| Out-File` 得到乱码，改成 `>` 重定向后完全正常。
"""
import sys

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

print(__doc__)
print("本工具已弃用：中文乱码的正解是「输出重定向到文件」，不是事后反解。")
raise SystemExit(1)
