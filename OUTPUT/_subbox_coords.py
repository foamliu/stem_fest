# -*- coding: utf-8 -*-
"""★ 8 个泄漏镜的**手工定框**（2026-09-16 逐帧读图 + 核对图反推确定）。

为什么手工：`_locate_subs.py` 自动检测会把碗、菜盘、书本误判为字幕
（镜 85 框到菜盘、镜 88 框到书本），**因此必须人工读图定框**。

读框方法：
  1. `_subtimeline.py --shot=N` 出底部 28% 时间线图，确认字幕**出现时段**与**位置固定性**
  2. `_delogo.py --shots=N --preview` 出红框核对图（原尺寸 1056×608，可直接读坐标）
  3. 按核对图微调 y（v1 首版普遍偏上 ~30px，已修正）

坐标 = 全图 1056×608 下的 (x, y, w, h)，取「字幕出现过的所有时段」并集 + 描边 padding。
"""
# 镜号 -> (x, y, w, h)
# v4 精修（2026-09-16，按 `_delogo_check/S*_before_after.jpg` 视觉终检结果）：
#   · 镜 85：v3 框（420×74）压到碗和桌面，抹出缺口 → 收窄到字幕实际范围（「吃了」居中，约 x=430..640）
#   · 镜 112：v3 框高度不足，字幕描边留残影 → 加高至覆盖完整两行
#   · 其余镜终检通过，保持不变
BOXES = {
    14:  (330, 502, 400, 66),    # ❌ delogo 不适用（白衣+红领巾高对比 → 竖条纹），待重跑
    21:  (395, 528, 268, 58),    # ✅ 保留率 3%
    36:  (370, 496, 340, 110),   # ✅ 保留率 0%
    77:  (350, 534, 356, 64),    # ✅ 保留率 0%
    85:  (420, 524, 240, 60),    # 收窄 → 只盖字幕，避开左侧碗
    86:  (352, 536, 348, 62),    # ✅ 保留率 0%
    88:  (440, 528, 196, 74),    # ✅ 保留率 23%（碗/桌底噪）
    112: (360, 540, 342, 66),    # 加高 → 盖住描边残影
}


def get(shot):
    return BOXES.get(shot)
